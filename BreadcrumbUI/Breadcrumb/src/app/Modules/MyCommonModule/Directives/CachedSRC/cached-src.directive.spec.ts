import { CachedSRCDirective } from './cached-src.directive';

describe('CachedSRCDirective', () => {
  it('should create an instance', () => {
    const directive = new CachedSRCDirective();
    expect(directive).toBeTruthy();
  });
});
